Docker is not installed in this workspace, so the exact VESSL/base image could not be extracted here.
If you know the image tag, record it here, for example:
pytorch/pytorch:2.4.0-cuda12.4-cudnn9-runtime